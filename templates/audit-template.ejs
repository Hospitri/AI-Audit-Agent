<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Hospitri — Audit</title>
  <style>
    :root{
      --bg: #ffffff;
      --panel: #F6F5EF;          /* gris interno */
      --ink: #111111;
      --muted: #666666;
      --border: #0f0f0f;
      --green-dark: #064541;
      --green-light: #E1F67A;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0; padding: 32px;
      background: var(--bg); color: var(--ink);
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
    }
    .sheet {
      max-width: 820px; margin: 0 auto;
      background: var(--panel);
      border: 2px solid var(--border);
      border-radius: 16px;
      padding: 28px 28px 24px;
    }

    /* Header */
    .header { text-align: center; }
    .logo {
      width: 48px; height: 48px; border-radius: 12px;
      display: inline-block; margin-bottom: 12px;
    }
    .brand {
      font-family: Georgia, 'Times New Roman', serif; /* fallback serif */
      font-size: 28px; font-weight: 800; letter-spacing: 0.2px;
      text-transform: lowercase;
      color: var(--green-dark);
      margin: 0 0 4px 0;
    }
    .kicker {
      margin: 0 0 8px 0;
      color: var(--muted);
      font-size: 14px;
    }
    .listing-title {
      font-family: Georgia, 'Times New Roman', serif;
      font-size: 20px; font-weight: 700;
      margin: 6px 0 10px 0;
    }

    /* Score & badges */
    .score-wrap {
      display: flex; align-items: center; justify-content: center;
      gap: 10px; margin: 8px 0 24px;
    }
    .score-badge {
      font-size: 36px; font-weight: 800;
      color: #fff; background: var(--green-dark);
      padding: 6px 18px; border-radius: 999px;
      line-height: 1.1;
    }
    .score-label { color: var(--muted); font-size: 14px; }

    /* Sections */
    h2 {
      font-family: Georgia, 'Times New Roman', serif;
      font-size: 18px; margin: 18px 0 10px;
      color: var(--green-dark);
    }
    .grid {
      display: grid; grid-template-columns: repeat(2, minmax(0,1fr));
      gap: 12px;
    }
    .card {
      background: #fff; border: 1px solid #e7e7e7; border-radius: 12px;
      padding: 12px;
    }
    .title {
      font-weight: 700; margin-bottom: 4px; text-transform: capitalize;
      color: var(--green-dark);
    }
    .kv { font-size: 13px; margin-top: 4px; }
    .muted { color: var(--muted); font-size: 12px; }

    /* Quick wins */
    .wins { margin: 6px 0 12px; padding-left: 18px; }
    .wins li { margin: 6px 0; }
    .chip {
      display: inline-block; font-size: 12px; padding: 2px 8px;
      border-radius: 999px; border: 1px solid #d9d9d9; background: #fff;
      margin-left: 6px;
    }
    .chip--effort { background: #fff; }
    .chip--impact { background: var(--green-light); border-color: #cfe56d; }

    /* Legend */
    .legend {
      font-size: 12px; color: var(--muted);
      background: #fff; border: 1px dashed #d9d9d9;
      padding: 8px 10px; border-radius: 10px; margin-top: 8px;
    }

    /* Footer */
    .footer {
      text-align: center; margin-top: 18px; color: var(--muted);
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="sheet">
    <div class="header">
      <img class="logo" src="https://framerusercontent.com/images/m3toUFui8qBDtUIrTvF9mXpuUM.svg" alt="Hospitri logo" />
      <h1 class="brand">hospitri</h1>
      <div class="kicker">Here’s your free audit!</div>

      <% const listingTitle = (audit && (audit.listing_title || (audit.meta && audit.meta.title))) || ''; %>
      <% if (listingTitle) { %>
        <div class="listing-title"><%= listingTitle %></div>
      <% } %>
    </div>

    <div class="score-wrap">
      <div class="score-badge"><%= audit.overall_score %></div>
      <div class="score-label">Overall score</div>
    </div>

    <h2>Category breakdown</h2>
    <div class="grid">
      <% for (const [k, v] of Object.entries(audit.category_breakdown || {})) { %>
        <div class="card">
          <div class="title"><%= k %></div>
          <div class="kv">Score: <b><%= v.numeric %></b> — Grade: <b><%= v.grade %></b></div>
          <div class="kv">What works: <%= v.what_works %></div>
          <div class="kv">Improve: <%= v.what_to_improve %></div>
        </div>
      <% } %>
    </div>

    <h2>Quick wins</h2>
    <ul class="wins">
      <% (audit.quick_wins || []).forEach(q => { %>
        <li>
          <b><%= q.action %></b>
          <span class="chip chip--effort">Effort: <%= q.effort %></span>
          <span class="chip chip--impact">Impact: <%= q.potential_uplift %><% if (q.potential_uplift_type) { %> (<%= q.potential_uplift_type %>)<% } %></span>
        </li>
      <% }) %>
    </ul>

    <div class="legend">
      Legend: <b>$</b> = revenue impact, <b>⚡</b> = guest-experience impact. More symbols = higher impact (e.g., <b>$$$</b>, <b>⚡⚡</b>).
    </div>

    <h2>Pro tip</h2>
    <p class="kv"><%= audit.pro_tip %></p>

    <div class="footer">© 2025 Hospitri LLC — Built with ♥ in Puerto Rico</div>
  </div>
</body>
</html>
